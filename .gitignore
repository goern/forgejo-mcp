gitea-mcp
gitea-mcp.exe

*.log

# Build output
build/
dist/
forgejo-mcp

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.envrc

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Claude Code settings
.claude/settings.local.json

.social-media/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Python bytecode cache
__pycache__/
worktrees/
.claude/scheduled_tasks.lock

# Secrets — defense-in-depth. Only SOPS-encrypted manifests (*.enc.yaml) and
# the cosign public key (intended to be committed) are allowed through.
secrets/*
!secrets/.gitkeep
!secrets/*.enc.yaml
!secrets/*.enc.yml
!secrets/cosign.pub
